# Phase 4 production trigger

This marker commit intentionally triggers the controlled GitHub Actions Production Golden Path.

Execution target: `https://www.defesai.shop`

The workflow only executes for this explicit `[PHASE4-RUN]` commit marker or manual dispatch.
