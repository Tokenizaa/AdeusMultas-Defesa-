# Evidência G0-01 — Estrutura de governança inicial

**Sessão:** 2026-08-24T05:37:54Z
**Feature:** G0-01

## Critérios de Aceite (de plan/features.json)

| Critério | Verificado | Observação |
|----------|-----------|------------|
| `loop/CHECKPOINT.md` existe | ✅ | Template com seções obrigatórias |
| `loop/loop.config.json` JSON válido | ✅ | `JSON.parse` OK; campos: max_sessions_per_day, smoke, invariants_glob |
| `plan/features.json` com ≥1 feature | ✅ | 14 features em G0–G6 |
| `loop/inbox.items.md` inicializado | ✅ | 2 itens abertos (semânticos, não bloqueantes a governança) |

## Estrutura criada

```
loop/
├── CHECKPOINT.md          ← template obrigatório para phase stop
├── loop.config.json       ← configuração do runtime
├── inbox.items.md         ← 2 itens abertos
├── sessions.log           ← 1 linha registrada (started)
├── checkpoints/           ← (vazio; arquivos checkpoint serão staged aqui por fase)
├── evidence/
│   ├── G0/                ← (este arquivo)
│   ├── G1/ … G6/          ← pastas preparadas
└── invariants/            ← (pasta-raiz; suites em tests/invariants/)

plan/
└── features.json          ← 14 features, 7 fases (G0–G6)
```

## Estado corrente

- **Fase corrente:** G0
- **Próxima feature:** G0-02 (suítes de invariantes)
- **Itens inbox:** INBOX-01 (SUPABASE_SERVICE_ROLE_KEY), INBOX-02 (PagBank testnet)

---
_Gerado pelo orquestrador. Evidência observada — não alegação._