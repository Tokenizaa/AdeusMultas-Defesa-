# Evidência G0-02 — Suítes de invariantes

**Sessão:** 2026-08-24T05:37:54Z
**Feature:** G0-02

## Suítes criadas

| Arquivo | Invariante | Ratchet |
|---------|-----------|---------|
| `tests/invariants/no-getSession-in-production.test.ts` | `getUser()` sempre, `getSession()` nunca em produção | Bloqueia feature se violada |
| `tests/invariants/rls-policies-exist.test.ts` | RLS habilitado em profiles + cases; ≥4 policies; migrations sequenciais sem buraco | Bloqueia feature se violada |
| `tests/invariants/no-raw-secrets-in-bundle.test.ts` | `.env.example` sem segredos reais; `.env` em `.gitignore` | Bloqueia feature se violada |

## Opcional: execução (depende de vitest instalado)

```bash
npx vitest run tests/invariants/
```

> Nota: execução adiada para esta sessão pois o objetivo primário é
> estruturar a governança. A suíte é um artefato versionado e o verifier
> a executará na verificação formal.

## Arquitetura de proteção (ratchet)

1. **PreToolUse hook**: bloqueia `Edit`/`Write`/`rm` em `tests/invariants/**`
2. **pre-commit hook** (`tests/invariants/`): roda `npx vitest run tests/invariants/` antes do commit
3. **Loop verifier**: inclui invariantes no battery a cada verificação de feature

---
_Gerado pelo orquestrador. Artefato versionado._